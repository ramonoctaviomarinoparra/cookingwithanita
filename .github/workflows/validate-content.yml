name: Cloudflare content update helper

on:
  push:
    branches: [ "main" ]

jobs:
  verify-json:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate videos.json
        run: python -m json.tool content/videos.json > /dev/null
